# tn-portal Environment Variables
# Copy this file to .env.local and configure for your local environment
# DO NOT commit .env.local to version control!

# ===================================
# API Configuration (Public - exposed to browser)
# ===================================

# API Base URL
PUBLIC_API_BASE_URL=http://localhost:8080

# API Request Timeout (milliseconds)
PUBLIC_API_TIMEOUT=30000

# ===================================
# Feature Flags (Public - optional)
# ===================================

# Enable experimental features
# PUBLIC_FEATURE_NEW_DASHBOARD=true
# PUBLIC_FEATURE_ANALYTICS=false

# ===================================
# SERVER-SIDE ONLY (Not exposed to browser)
# ===================================
# These variables should ONLY be used in server-side code
# They are NOT prefixed with PUBLIC_ and won't be in the browser bundle

# AZURE SSO Credentials (Backend only!)
SSO_CLIENT_ID=<replace with SSO client ID from 1Password>
SSO_TENANT_ID=<replace with SSO tenant ID from 1Password>
SSO_CLIENT_SECRET=<replace with SSO client secret from 1Password>
SSO_CUSTOM_SCOPE=read.user

# ===================================
# NOTES
# ===================================
# - Variables with PUBLIC_ prefix are exposed to browser JavaScript
# - Variables WITHOUT PUBLIC_ prefix are only available server-side
# - Rsbuild provides built-in variables:
#   - import.meta.env.MODE (development/production)
#   - import.meta.env.DEV (boolean)
#   - import.meta.env.PROD (boolean)